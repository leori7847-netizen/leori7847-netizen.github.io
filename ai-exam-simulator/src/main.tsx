// The deployed first-stage simulator is implemented as a static SPA in ../../app.js
// so GitHub Pages can serve it without a build step. This file keeps the requested
// React/Vite migration entry point available for the next phase.
export {};
